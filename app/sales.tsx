// Both server and frontend
export default function Sales() {
  // React component displayed at /sales
}

// Server only
export function loader() {
  // Fetches and returns data to Sales component
}

// Server only
export function action() {
  // Handles form submissions
}

export function links() {
  // Defines <link> elements
}

export function meta() {
  // Returns metadata
}

export function ErrorBoundary() {
  // Component that is displayed when errors occur
}
